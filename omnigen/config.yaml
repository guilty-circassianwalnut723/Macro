# OmniGen experiment configuration file
# All experiment configurations use YAML format

# Global configuration
global:
  # T2I data configuration (using example data)
  t2i_data_path: data/raw/t2i_example/t2i_example.jsonl
  # Multi-reference image data root directory
  multiref_data_root: data/filter
  # Default model configuration
  default_max_ref_images: 10
  # Input image pixel limit
  default_max_input_pixels: [1048576, 1048576, 589824, 589824, 589824, 262144, 262144, 262144, 262144, 262144]
  # Output image pixel limit
  default_max_output_pixels: 1048576
  # Default training parameters
  default_num_epochs: 10
  default_learning_rate: 8e-7
  default_batch_size: 1
  default_global_batch_size: 64
  default_gradient_accumulation_steps: 2
  default_t2i_ratio: 0.2

# Data configuration templates
data_config_templates:
  debug_config: &debug_config
    "1-3":
      data_num: 1000
    "4-5":
      data_num: 1000

# Experiment configuration list
experiments:
  example_with_t2i:
    use_t2i: true
    t2i_ratio: 0.1
    data_config:
      customization: *debug_config
      spatial: *debug_config
      illustration: *debug_config
      temporal: *debug_config
